---
import EditorialLayout from '../../../../../layouts/EditorialLayout.astro';
import RidingPage from '../../../../../components/riding/RidingPage.astro';
import { getAllOntarioRidings } from '../../../../../lib/riding-adapters/ontario';

export function getStaticPaths() {
  return getAllOntarioRidings().map((data) => ({
    params: { id: data.slug },
    props: { data },
  }));
}

const { data } = Astro.props;
const canonical = `https://vote-scope.com/es/canada/ontario/distritos/${data.slug}/`;
const alternateEn = `https://vote-scope.com/en/canada/ontario/ridings/${data.slug}/`;
const alternateFr = `https://vote-scope.com/fr/canada/ontario/circonscriptions/${data.slug}/`;
const title = `${data.name.en || data.name.fr} — Proyección provincial (Ontario) — Vote-Scope`;
const description = `Proyección Vote-Scope para ${data.name.en || data.name.fr} (Ontario): cuota de voto, probabilidad de victoria, diputado·a en funciones y candidatos 2025.`;
---

<EditorialLayout
  title={title}
  description={description}
  canonical={canonical}
  lang="es"
  currentPage="canada"
  ogImage="https://vote-scope.com/og/vote-scope.png"
  alternates={{ en: alternateEn, fr: alternateFr, es: canonical }}
  jsonLd={[
    {
      '@context': 'https://schema.org',
      '@type': 'Place',
      name: data.name.en || data.name.fr,
      containedInPlace: { '@type': 'AdministrativeArea', name: 'Ontario' },
      url: canonical,
    },
    {
      '@context': 'https://schema.org',
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Vote-Scope', item: 'https://vote-scope.com/es/' },
        { '@type': 'ListItem', position: 2, name: 'Canadá',     item: 'https://vote-scope.com/es/canada/' },
        { '@type': 'ListItem', position: 3, name: 'Ontario',    item: 'https://vote-scope.com/es/canada/ontario/' },
        { '@type': 'ListItem', position: 4, name: 'Distritos',  item: 'https://vote-scope.com/es/canada/ontario/distritos/' },
        { '@type': 'ListItem', position: 5, name: data.name.en || data.name.fr, item: canonical },
      ],
    },
  ]}
>
  <RidingPage data={data} lang="es" />
</EditorialLayout>
