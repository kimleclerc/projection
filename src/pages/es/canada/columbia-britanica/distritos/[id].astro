---
import EditorialLayout from '../../../../../layouts/EditorialLayout.astro';
import RidingPage from '../../../../../components/riding/RidingPage.astro';
import { getAllBcRidings } from '../../../../../lib/riding-adapters/bc';

export function getStaticPaths() {
  return getAllBcRidings().map((data) => ({
    params: { id: data.slug },
    props: { data },
  }));
}

const { data } = Astro.props;
const canonical = `https://vote-scope.com/es/canada/columbia-britanica/distritos/${data.slug}`;
const alternates = { en: `https://vote-scope.com/en/canada/british-columbia/ridings/${data.slug}`, fr: `https://vote-scope.com/fr/canada/colombie-britannique/circonscriptions/${data.slug}`, es: `https://vote-scope.com/es/canada/columbia-britanica/distritos/${data.slug}` };
const name = data.name.en;
const title = `${name} — Proyección provincial (Columbia Británica) — Vote-Scope`;
const description = `Vote-Scope — ${name} : cuotas de voto, probabilidad de victoria, diputado en funciones y candidaturas de 2024.`;
---

<EditorialLayout
  searchIndex={false}
  title={title}
  description={description}
  canonical={canonical}
  lang="es"
  currentPage="canada"
  alternates={alternates}
  jsonLd={[
    {
      '@context': 'https://schema.org',
      '@type': 'Place',
      name,
      containedInPlace: { '@type': 'AdministrativeArea', name: 'British Columbia' },
      url: canonical,
    },
    {
      '@context': 'https://schema.org',
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Vote-Scope', item: 'https://vote-scope.com/es/' },
        { '@type': 'ListItem', position: 2, name: 'Canadá', item: 'https://vote-scope.com/es/canada/' },
        { '@type': 'ListItem', position: 3, name: 'Columbia Británica', item: 'https://vote-scope.com/es/canada/columbia-britanica/' },
        { '@type': 'ListItem', position: 4, name: 'Distritos', item: 'https://vote-scope.com/es/canada/columbia-britanica/distritos/' },
        { '@type': 'ListItem', position: 5, name, item: canonical },
      ],
    },
  ]}
>
  <RidingPage data={data} lang="es" />
</EditorialLayout>
