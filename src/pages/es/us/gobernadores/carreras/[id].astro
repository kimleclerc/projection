---
import EditorialLayout from '../../../../../layouts/EditorialLayout.astro';
import RidingPage from '../../../../../components/riding/RidingPage.astro';
import { getAllUSGovernorRaces } from '../../../../../lib/riding-adapters/us-governor';

export function getStaticPaths() {
  return getAllUSGovernorRaces().map((data) => ({
    params: { id: data.slug },
    props: { data },
  }));
}

const { data } = Astro.props;
const canonical = `https://vote-scope.com/es/us/gobernadores/carreras/${data.slug}/`;
const title = `${data.name.en} — gobernación 2026 — Vote-Scope`;
const description = `Proyección de Vote-Scope para la gobernación de ${data.name.en}: voto, probabilidad de victoria, resultado anterior y si el puesto queda abierto.`;
---

<EditorialLayout
  title={title}
  description={description}
  canonical={canonical}
  lang="es"
  currentPage="us"
  ogImage="https://vote-scope.com/og/vote-scope.png"
  alternates={{
    en: `https://vote-scope.com/en/us/governors/races/${data.slug}/`,
    fr: `https://vote-scope.com/fr/us/gouverneurs/courses/${data.slug}/`,
    es: `https://vote-scope.com/es/us/gobernadores/carreras/${data.slug}/`,
  }}
  jsonLd={[
    {
      '@context': 'https://schema.org',
      '@type': 'Place',
      name: data.name.en,
      containedInPlace: { '@type': 'AdministrativeArea', name: 'United States' },
      url: canonical,
    },
    {
      '@context': 'https://schema.org',
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Vote-Scope',   item: 'https://vote-scope.com/es/' },
        { '@type': 'ListItem', position: 2, name: 'EE. UU.',   item: 'https://vote-scope.com/es/us/' },
        { '@type': 'ListItem', position: 3, name: 'Gobernadores',  item: 'https://vote-scope.com/es/us/gobernadores/' },
        { '@type': 'ListItem', position: 4, name: 'Contiendas', item: 'https://vote-scope.com/es/us/gobernadores/carreras/' },
        { '@type': 'ListItem', position: 5, name: data.name.en, item: canonical },
      ],
    },
  ]}
>
  <RidingPage data={data} lang="es" />
</EditorialLayout>
