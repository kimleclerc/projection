---
import EditorialLayout from '../../../layouts/EditorialLayout.astro';
import FranceCandidacyEffect from '../../../components/FranceCandidacyEffect.astro';
import effects from '../../../../web_data/france-presidential/candidacy_effects.json';

const m = (effects as any).meta;
// La plus grosse sortie mesurée donne la phrase de la description.
const top = ((effects as any).exits ?? [])
  .filter((e: any) => e.destinations?.length)
  .sort((a: any, b: any) => b.own.mean - a.own.mean)[0];
const lead = top ? `Sans ${top.short}, ${Math.round(top.destinations[0].share_of_votes * 100)} % de ses électeurs vont à ${top.destinations[0].short}. ` : '';
const canonical = 'https://vote-scope.com/fr/france/effet-candidature/';
---

<EditorialLayout
  title="Présidentielle 2027 : à qui chaque candidature prend ses voix — Vote-Scope"
  description={`À qui chaque candidature prend ses voix, mesuré dans les sondages eux-mêmes. ${lead}Calcul sur ${m.n_polls} sondages des douze derniers mois.`}
  canonical={canonical}
  lang="fr"
  currentPage="france"
  ogImage="https://vote-scope.com/og/france-desk/fr.png"
  alternates={{ en: 'https://vote-scope.com/en/france/candidacy-effect/', fr: 'https://vote-scope.com/fr/france/effet-candidature/', es: 'https://vote-scope.com/es/france/efecto-candidatura/' }}
  jsonLd={[{
    '@context': 'https://schema.org',
    '@type': 'Dataset',
    name: 'Présidentielle 2027 : à qui chaque candidature prend ses voix',
    url: canonical,
    dateModified: m.run_date,
    creator: { '@type': 'Person', name: 'Kim Leclerc', url: 'https://kimleclerc.ca' },
    publisher: { '@type': 'Organization', name: 'Vote-Scope', url: 'https://vote-scope.com' },
    isAccessibleForFree: true,
  }]}
>
  <FranceCandidacyEffect locale="fr" />
</EditorialLayout>
